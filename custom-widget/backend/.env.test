# Test environment variables
NODE_ENV=test
PORT=3003

# PostgreSQL database for tests (Phase 2: PostgreSQL-only)
DATABASE_URL="postgresql://simonaszilinskas@localhost:5432/vilnius_support_test"

# Test AI configuration (use mock or test keys)
AI_PROVIDER=openrouter
OPENROUTER_API_KEY=test-key
OPENROUTER_MODEL=google/gemini-2.5-flash
MISTRAL_API_KEY=test-mistral-key

# Test RAG configuration
RAG_K=5
RAG_SHOW_SOURCES=true

# Test Chroma DB (use test collection)
CHROMA_URL=https://api.trychroma.com
CHROMA_TENANT=test-tenant
CHROMA_DATABASE=test-database
CHROMA_AUTH_TOKEN=test-token

# JWT secrets for testing
JWT_SECRET=test-jwt-secret-key
JWT_REFRESH_SECRET=test-refresh-secret-key
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Widget test configuration
WIDGET_NAME=Test Widget
WIDGET_PRIMARY_COLOR=#fc030b
WIDGET_ALLOWED_DOMAINS=*

# Disable external services in tests
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=test@example.com